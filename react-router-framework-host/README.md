# React Router Framework with Module Federation - Host App

This host app uses the [module-federation/vite](https://github.com/module-federation/vite) plugin to consume a federated remote modules.

## Install

```
npm install
```

## Run

```
npm run dev
```
